# This file marks the `src/` directory as a Python package.
# It can also be used to define package-level imports or metadata if needed.

# Example (optional):
# from .password_checker import PasswordChecker
